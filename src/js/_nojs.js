// ========================================================
// no-js
// ========================================================

const Nojs = target => {

  const _tar = document.getElementsByTagName(target);
  _tar[0].classList.remove('no-js');

};

export default Nojs;
